# dummy-mcp-server-rust

Same dummy MCP server as the Python fixtures, built with the official
[Rust MCP SDK](https://github.com/modelcontextprotocol/rust-sdk) (`rmcp`)
instead of FastMCP. See the [repo root README](../README.md) for the full
set of fixtures.

## Run locally

```bash
cargo run
```
